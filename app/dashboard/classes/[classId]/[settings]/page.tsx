import React from 'react'

const GradeSettings = ({ params }: { params: { settingsId: string } }) => {

    const { settingsId } = params;

  return (
    <div className='w-full min-h-screen'>

    </div>
  )
}

export default GradeSettings